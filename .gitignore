# Gradle
.gradle/
build/

# Local, machine-specific configuration (contains the Android SDK path)
local.properties

# Kotlin/Native
.kotlin/

# Xcode / iOS
iosApp/build/
xcuserdata/
*.xcworkspace/xcuserdata/

# Fastlane (generated on every run; never commit credentials)
iosApp/fastlane/report.xml
iosApp/fastlane/Preview.html
iosApp/fastlane/test_output/
iosApp/fastlane/*.p8
iosApp/fastlane/*.mobileprovision
iosApp/fastlane/app_store_connect_api_key.json

# OS / IDE
.DS_Store
.idea/

# Symlinks to checkouts outside this repo
/iphone-buils

# Signing material and CI secrets — never commit
.env
*.p12
*.p8
*.mobileprovision
apple-signing
.playwright-mcp
# Firebase config for Android push — per-deployment, not a repository artifact
composeApp/google-services.json
